{
  "version": 2,
  "builds": [
    {
      "src": "nuxt.config.js",
      "use": "@nuxtjs/vercel-builder",
      "config": {
        "serverFiles": ["src/queries/**"]
      }
    }
  ],
  "rewrites": [
    { "source": "//:match*", "destination": process.env.CRAFT_BASE_URL +,:match*" }
  ]
}
